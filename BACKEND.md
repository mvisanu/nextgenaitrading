# NextGenStock — Backend Reference

> **Disclaimer:** NextGenStock is educational software. Live trading with real broker
> credentials carries real financial risk. The authors accept no liability for trading
> losses. Always test with paper accounts before enabling live execution.

---

## Table of Contents

1. [Running the backend locally](#running-the-backend-locally)
2. [Environment variables](#environment-variables)
3. [Database migration instructions](#database-migration-instructions)
4. [API endpoint reference](#api-endpoint-reference)
5. [Architecture decisions](#architecture-decisions)
6. [Deviations from spec](#deviations-from-spec)

---

## Running the backend locally

### Prerequisites

- Python 3.12+
- PostgreSQL 15+ running locally (or a Supabase connection string)
- `pip` or a virtual environment manager

### Setup steps

```bash
# 1. Create and activate a virtual environment
cd backend
python -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate

# 2. Install dependencies
pip install -r requirements.txt

# 3. Create your .env file
cp .env.example .env
# Edit .env and fill in DATABASE_URL, SECRET_KEY, ENCRYPTION_KEY

# 4. Generate the Fernet encryption key (run once)
python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Paste the output as ENCRYPTION_KEY in .env

# 5. Run Alembic migrations
alembic upgrade head

# 6. Start the server
uvicorn app.main:app --reload --port 8000
```

The API will be available at:
- `http://localhost:8000` — API base
- `http://localhost:8000/docs` — Swagger UI
- `http://localhost:8000/redoc` — ReDoc
- `http://localhost:8000/healthz` — Render health check endpoint

---

## Environment variables

All variables are loaded via `pydantic-settings` from `.env`.

| Variable | Required | Default | Description |
|---|---|---|---|
| `DATABASE_URL` | Yes | — | `postgresql+asyncpg://user:pass@host/db` |
| `SECRET_KEY` | Yes | — | HMAC secret for JWT signing (min 32 chars) |
| `JWT_ALGORITHM` | No | `HS256` | JWT signing algorithm |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | No | `15` | Access token lifetime |
| `REFRESH_TOKEN_EXPIRE_DAYS` | No | `7` | Refresh token lifetime |
| `ENCRYPTION_KEY` | Yes | — | Fernet key for broker credential encryption |
| `CORS_ORIGINS` | No | `http://localhost:3000` | Comma-separated allowed CORS origins |
| `COOKIE_SECURE` | No | `false` | Set `true` in production (requires HTTPS) |
| `COOKIE_SAMESITE` | No | `lax` | Cookie SameSite attribute |
| `POOL_SIZE` | No | `5` | SQLAlchemy connection pool size |
| `MAX_OVERFLOW` | No | `10` | SQLAlchemy pool max overflow |
| `ALPACA_BASE_URL` | No | `https://api.alpaca.markets` | Alpaca live API URL |
| `ALPACA_PAPER_URL` | No | `https://paper-api.alpaca.markets` | Alpaca paper API URL |
| `ROBINHOOD_BASE_URL` | No | `https://trading.robinhood.com` | Robinhood base URL |

---

## Database migration instructions

```bash
# Generate a migration after changing ORM models
alembic revision --autogenerate -m "describe your change"

# Review the generated file in alembic/versions/ before applying

# Apply pending migrations
alembic upgrade head

# Rollback one migration
alembic downgrade -1

# Check current revision
alembic current

# View migration history
alembic history --verbose
```

**Important:** Always review autogenerated migrations before applying. Alembic may miss
some index additions or custom constraint details. Add them manually if needed.

---

## API endpoint reference

### Auth — `/auth`

| Method | Path | Auth | Request | Response |
|---|---|---|---|---|
| `POST` | `/auth/register` | No | `{email, password}` | `{message, user_id, email}` + HTTP-only cookies |
| `POST` | `/auth/login` | No | `{email, password}` | `{message, user_id, email}` + HTTP-only cookies |
| `POST` | `/auth/refresh` | Cookie | — | `{message, user_id, email}` + rotated cookies |
| `POST` | `/auth/logout` | Cookie | — | `204 No Content`, cookies cleared |
| `GET` | `/auth/me` | Cookie | — | `{id, email, is_active, created_at}` |

### Profile — `/profile`

| Method | Path | Auth | Request | Response |
|---|---|---|---|---|
| `GET` | `/profile` | Yes | — | `{id, user_id, display_name, timezone, default_symbol, default_mode}` |
| `PATCH` | `/profile` | Yes | `{display_name?, timezone?, default_symbol?, default_mode?}` | Updated profile |

### Broker Credentials — `/broker/credentials`

| Method | Path | Auth | Request | Response |
|---|---|---|---|---|
| `GET` | `/broker/credentials` | Yes | — | `[{id, provider, profile_name, api_key_masked, ...}]` |
| `POST` | `/broker/credentials` | Yes | `{provider, profile_name, api_key, secret_key, paper_trading?}` | Created credential (masked) |
| `PATCH` | `/broker/credentials/{id}` | Yes | `{profile_name?, api_key?, secret_key?, ...}` | Updated credential |
| `DELETE` | `/broker/credentials/{id}` | Yes | — | `204 No Content` |
| `POST` | `/broker/credentials/{id}/test` | Yes | — | `{ok: bool, detail?}` |

**Note:** `api_key` in all responses is masked as `****(encrypted)`. Decrypted values are never returned.

### Strategies — `/strategies`

| Method | Path | Auth | Request | Response |
|---|---|---|---|---|
| `POST` | `/strategies/ai-pick/run` | Yes | `{symbol, timeframe, mode, leverage?, dry_run}` | `StrategyRunOut` |
| `POST` | `/strategies/buy-low-sell-high/run` | Yes | Same | `StrategyRunOut` |
| `GET` | `/strategies/runs` | Yes | `?limit=50` | `[StrategyRunOut]` |
| `GET` | `/strategies/runs/{id}` | Yes | — | `StrategyRunOut` |
| `GET` | `/strategies/runs/{id}/decisions` | Yes | `?limit=100` | `[TradeDecisionOut]` |

### Backtests — `/backtests`

| Method | Path | Auth | Request | Response |
|---|---|---|---|---|
| `POST` | `/backtests/run` | Yes | `{symbol, timeframe, mode, leverage?}` | `BacktestOut` |
| `GET` | `/backtests` | Yes | `?limit=50` | `[BacktestOut]` |
| `GET` | `/backtests/{id}` | Yes | — | `BacktestOut` |
| `GET` | `/backtests/{id}/trades` | Yes | — | `[BacktestTradeOut]` |
| `GET` | `/backtests/{id}/leaderboard` | Yes | — | `[LeaderboardEntry]` ranked by `validation_score` desc |
| `GET` | `/backtests/{id}/chart-data` | Yes | — | `{candles, signals, equity}` |

### Live Trading — `/live`

| Method | Path | Auth | Request | Response |
|---|---|---|---|---|
| `POST` | `/live/run-signal-check` | Yes | `{symbol, timeframe, mode, credential_id, dry_run}` | `StrategyRunOut` |
| `POST` | `/live/execute` | Yes | `{symbol, side, quantity?, notional_usd?, credential_id, dry_run}` | `OrderOut` |
| `GET` | `/live/orders` | Yes | `?limit=50` | `[OrderOut]` |
| `GET` | `/live/positions` | Yes | — | `[PositionOut]` |
| `GET` | `/live/status` | Yes | `?credential_id=` | `AccountStatus` |
| `GET` | `/live/chart-data` | Yes | `?symbol=&interval=` | `{candles}` |

### Artifacts — `/artifacts`

| Method | Path | Auth | Request | Response |
|---|---|---|---|---|
| `GET` | `/artifacts` | Yes | `?limit=50` | `[ArtifactOut]` |
| `GET` | `/artifacts/{id}` | Yes | — | `ArtifactOut` |
| `GET` | `/artifacts/{id}/pine-script` | Yes | — | `{id, variant_name, symbol, pine_script_version, pine_script_code}` |

---

## Architecture decisions

### Auth — HTTP-only cookies + SHA-256 refresh token hash

Access tokens (15 min) and refresh tokens (7 days) are issued as HTTP-only, SameSite=Lax cookies.
Tokens are **never** exposed to JavaScript. The refresh token itself is never stored in the DB —
only its SHA-256 hex hash is stored in `user_sessions.refresh_token_hash`. This means a DB breach
does not expose refresh tokens. On refresh, the incoming token is hashed and compared.

### Multi-tenancy

Every user-owned table has a `user_id` FK with `ondelete="CASCADE"`. Every query in every service
method is scoped with `WHERE user_id = current_user.id`. The `assert_ownership(record_user_id, current_user_id)`
utility in `app/core/security.py` raises HTTP 403 if IDs don't match. It is called in every
service function before returning or modifying a record.

### Broker credential encryption

Both `api_key` and `encrypted_secret_key` columns store Fernet-encrypted ciphertext.
The `ENCRYPTION_KEY` env var is the Fernet symmetric key. Decryption happens only inside
`broker/factory.py::get_broker_client()` at execution time, in-memory, never logged.
API responses return `****(encrypted)` for the api_key field.

### Strategy execution model

- **Conservative / Aggressive**: HMM (GaussianHMM, 2 states) fitted on 730 days of log-returns,
  ATR, and volume ratio. Bull/bear state identified by comparing state mean returns. 8 confirmation
  indicators gate entry. Saved as `StrategyRun` + `TradeDecision` records.

- **AI Pick**: 12-variant grid over MACD fast/slow, RSI window, EMA short/long, RSI oversold.
  Each variant is backtested on a 60/20/20 split. Ranked by `validation_score = validation_return / (1 + max_drawdown)`.
  Winner generates a Pine Script v5 artifact.

- **Buy Low / Sell High**: 8-variant grid over RSI oversold level, Bollinger window, cycle hold bars.
  Dip entry (RSI < oversold AND price < BB lower), cycle exit (RSI > 65 OR price > BB upper).
  Same ranking and artifact generation as AI Pick.

### Backtesting engine

The engine in `app/backtesting/engine.py` operates on a pre-computed signal list aligned with the
OHLCV DataFrame. Features:
- 60/20/20 train/validation/test split
- Leverage applied multiplicatively to each bar return
- Cooldown: N bars after exit before re-entry allowed
- Trailing stop: tracks highest price seen since entry, exits when price drops X% from peak
- `validation_score = validation_return / (1 + max_drawdown / 100)` — risk-adjusted ranking

### Async everywhere

All route handlers and service functions are `async`. The database layer uses `AsyncSession` with
`async_sessionmaker` and `asyncpg` driver. No sync calls block the event loop.

### 4h timeframe resampling

yfinance does not natively support `4h` as an interval. When `timeframe="4h"` is requested,
the market data service fetches `1h` bars and resamples to 4h using pandas `resample("4h").agg(...)`.

---

## Deviations from spec

| # | Deviation | Reason |
|---|---|---|
| 1 | `POST /strategies/ai-pick/run` uses 202 Accepted | These runs can take up to 120 seconds; 202 communicates async work semantics even though the response is synchronous in v1. |
| 2 | Refresh token stored as SHA-256 (not bcrypt) | bcrypt is intentionally slow (appropriate for passwords); refresh tokens are long random JWTs where SHA-256 is sufficient and much faster. |
| 3 | `GET /live/positions` returns DB snapshot, not live broker positions | Live broker polling on every GET request would add latency and rate-limit risk. A future endpoint can proxy `client.get_positions()` directly. The DB snapshot is populated when `POST /live/execute` completes. |
| 4 | RobinhoodClient is a stub (all methods raise NotImplementedError except ping) | The Robinhood official crypto API requires Ed25519 key signing and is not yet implemented. `ping()` returns False. Dry-run orders return a simulated response. |
| 5 | 4h timeframe resampled from 1h | yfinance does not expose a native 4h interval. Resampling from 1h is the standard workaround. |
| 6 | `assert_ownership` placed in `core/security.py` (not a separate util file) | Consistent with TASKS.md clarification "OQ-20: assert_ownership placement". |
| 7 | `GET /strategies/runs/{id}/decisions` endpoint added (not in original spec) | Required by frontend to render per-bar signal markers on the price chart. |
| 8 | Pine Script approximates HMM regime as EMA alignment | HMM requires a Python runtime and cannot be ported to Pine Script. EMA short > EMA long is the closest deterministic approximation. The Pine Script includes a comment explaining this. |
